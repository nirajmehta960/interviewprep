import type { AuthoredProblem } from "@/lib/types";

/**
 * The eight number drills worth keeping from `~/Documents/DSA JAVA/JavaBasics`.
 *
 * The other seventeen files there are I/O and syntax exercises — average,
 * calculator, ternary, week-day, loops — with no interview counterpart, and the
 * nine `Patterns` files are nested-loop printing. Those are deliberately not
 * included.
 *
 * Four of these eight had real defects, each named in its `criticalTrap`:
 * `palindrome-number` (negatives), `math-is-prime` (1 reported prime),
 * `math-binomial-coefficient` (factorial overflow from n = 13) and
 * `math-decimal-to-binary` (int overflow from 1024).
 */
export const numberDrills: AuthoredProblem[] = [
  {
    slug: "palindrome-number", number: 9, title: "Palindrome Number", difficulty: "EASY",
    categorySlug: "math", patternSlugs: ["math"],
    synopsis: "Reverse the digits and compare — but reject negatives first.",
    description: [
      "Given an integer, return true if it reads the same backwards as forwards.",
    ],
    examples: [
      { label: "palindrome", input: "x = 121", output: "true", explanation: "121 reversed is 121." },
      { label: "negative", input: "x = -121", output: "false", explanation: "Reading backwards gives 121- , so no negative number is a palindrome." },
      { label: "trailing zero", input: "x = 10", output: "false", explanation: "01 reversed is 1, which differs from 10." },
    ],
    constraints: [
      "-2³¹ ≤ x ≤ 2³¹ - 1",
    ],
    intuition:
      "Peeling digits is the whole technique: `% 10` reads the last digit, `/ 10` removes it, and `reverse * 10 + digit` rebuilds the number backwards. Then compare. The two things that make this a real question rather than a one-liner are the sign — the minus sign is not symmetric, so no negative number qualifies — and the fact that reversing a 10-digit int can exceed `Integer.MAX_VALUE` even when the input does not.",
    approach: [
      "Reject negatives immediately: return false.",
      "Copy the input so the original survives for the final comparison.",
      "While the copy is non-zero, take `% 10` and fold it into the reverse.",
      "Compare the reverse with the original.",
      "Use a `long` accumulator, or compare only half the digits, to avoid overflow.",
    ],
    timeComplexity: "O(log₁₀ n) — one step per digit",
    spaceComplexity: "O(1)",
    keyTakeaway:
      "Guard the sign before anything else, and accumulate into a long. Both problems are invisible on the textbook input 121 and appear immediately on -121 and on any large value.",
    revisionSummary: [
      "% 10 reads the last digit, / 10 drops it.",
      "Negatives are never palindromes — reject up front.",
      "Reversing can overflow int even when the input does not; use long.",
    ],
    criticalTrap:
      "The original omits the negative check, so `-121` reverses to `-121` — Java gives `-121 % 10 == -1` — and it wrongly returns **true**. Verified: `-121` and `-1` both report true where LeetCode 9 requires false.",
    leetcodeUrl: "https://leetcode.com/problems/palindrome-number/",
    languages: ["JAVA", "PYTHON"],
    solutions: [
      { language: "JAVA", code: [
          "class Solution {",
          "    public boolean isPalindrome(int number) {",
          "        // No negative number is a palindrome: the minus sign is not symmetric.",
          "        if (number < 0) {",
          "            return false;",
          "        }",
          "",
          "        int palindrome = number;",
          "        long reverse = 0;   // long: reversing an int can overflow int",
          "",
          "        while (palindrome != 0) {",
          "            int remainder = palindrome % 10;",
          "            reverse = reverse * 10 + remainder;",
          "            palindrome = palindrome / 10;",
          "        }",
          "",
          "        return number == reverse;",
          "    }",
          "}",
        ].join("\n") },
      { language: "PYTHON", code: [
          "class Solution:",
          "    def is_palindrome(self, number: int) -> bool:",
          "        # No negative number is a palindrome.",
          "        if number < 0:",
          "            return False",
          "",
          "        palindrome = number",
          "        reverse = 0",
          "",
          "        while palindrome != 0:",
          "            remainder = palindrome % 10",
          "            reverse = reverse * 10 + remainder",
          "            palindrome = palindrome // 10",
          "",
          "        return number == reverse",
        ].join("\n") },
    ],
  },
  {
    slug: "reverse-integer", number: 7, title: "Reverse Integer", difficulty: "MEDIUM",
    categorySlug: "math", patternSlugs: ["math"],
    synopsis: "Pull digits off the back — and detect the overflow before it happens.",
    description: [
      "Given a signed 32-bit integer, reverse its digits. If reversing causes the value to fall outside the signed 32-bit range, return 0.",
      "You may not use a 64-bit type to store the intermediate result — which is what makes this more than a digit-peeling exercise.",
    ],
    examples: [
      { label: "positive", input: "x = 123", output: "321", explanation: "Straightforward digit reversal." },
      { label: "negative", input: "x = -123", output: "-321", explanation: "The sign is preserved." },
      { label: "overflows", input: "x = 1534236469", output: "0", explanation: "The reverse exceeds 2³¹ - 1, so the answer is 0." },
    ],
    constraints: [
      "-2³¹ ≤ x ≤ 2³¹ - 1",
    ],
    intuition:
      "The digit-peeling loop is the same as the palindrome drill; the interesting part is the overflow check. Since you cannot widen to `long`, the test has to happen *before* the multiplication: if the accumulator already exceeds `MAX_VALUE / 10`, then multiplying by ten will certainly overflow. Checking afterwards is useless because the overflow has already wrapped the value silently.",
    approach: [
      "Loop while the value is non-zero, using `% 10` and `/ 10`.",
      "Before each `reverse = reverse * 10 + digit`, check whether `reverse` exceeds `Integer.MAX_VALUE / 10`.",
      "Also handle the boundary case where it equals that quotient and the incoming digit would push it over.",
      "Return 0 on overflow.",
      "Java division truncates toward zero, so negatives need no special handling.",
    ],
    timeComplexity: "O(log₁₀ n)",
    spaceComplexity: "O(1)",
    keyTakeaway:
      "Check for overflow before multiplying, not after. Once an int has wrapped there is no way to detect that it did — the value simply becomes wrong.",
    revisionSummary: [
      "Same % 10 and / 10 peeling as the palindrome drill.",
      "Test reverse > MAX_VALUE / 10 before each multiply.",
      "Truncation toward zero means the sign takes care of itself.",
    ],
    criticalTrap:
      "The original loops on `while (n > 0)`, so every negative input returns 0 immediately, and it performs no overflow check at all. Both are fine for a console exercise and both fail LeetCode 7.",
    leetcodeUrl: "https://leetcode.com/problems/reverse-integer/",
    languages: ["JAVA", "PYTHON"],
    solutions: [
      { language: "JAVA", code: [
          "class Solution {",
          "    public int reverse(int n) {",
          "        int reverse = 0;",
          "",
          "        // != 0, not > 0: Java truncates toward zero so negatives work.",
          "        while (n != 0) {",
          "            int lastDigit = n % 10;",
          "",
          "            // Check BEFORE multiplying — afterwards it has already wrapped.",
          "            if (reverse > Integer.MAX_VALUE / 10",
          "                    || reverse < Integer.MIN_VALUE / 10) {",
          "                return 0;",
          "            }",
          "",
          "            reverse = reverse * 10 + lastDigit;",
          "            n = n / 10;",
          "        }",
          "",
          "        return reverse;",
          "    }",
          "}",
        ].join("\n") },
      { language: "PYTHON", code: [
          "class Solution:",
          "    INT_MAX = 2 ** 31 - 1",
          "    INT_MIN = -(2 ** 31)",
          "",
          "    def reverse(self, n: int) -> int:",
          "        sign = -1 if n < 0 else 1",
          "        n = abs(n)",
          "        reverse = 0",
          "",
          "        while n != 0:",
          "            reverse = reverse * 10 + n % 10",
          "            n //= 10",
          "",
          "        reverse *= sign",
          "",
          "        # Python ints are unbounded, so the range check is explicit.",
          "        if reverse < self.INT_MIN or reverse > self.INT_MAX:",
          "            return 0",
          "",
          "        return reverse",
        ].join("\n") },
    ],
  },
  {
    slug: "math-is-prime", number: null, title: "Check if a Number is Prime", difficulty: "EASY",
    categorySlug: "math", patternSlugs: ["math"],
    synopsis: "Trial division to the square root — and the guard that 1 is not prime.",
    description: [
      "Return true if a number is prime: greater than 1 and divisible only by 1 and itself.",
    ],
    examples: [
      { label: "prime", input: "n = 17", output: "true", explanation: "No divisor up to 4 divides it." },
      { label: "composite", input: "n = 25", output: "false", explanation: "5 divides it, and 5 ≤ √25." },
      { label: "one", input: "n = 1", output: "false", explanation: "1 has only one divisor, so by definition it is not prime." },
    ],
    constraints: [
      "Practical for n up to about 10¹²",
    ],
    intuition:
      "Stopping at √n is the whole optimisation, and the reason is a pairing argument: if `n = a × b` then one of the two factors must be at most √n, so any composite number is guaranteed to have a divisor in that range. Testing beyond it can only rediscover the larger partner of a factor you already checked. That takes the work from O(n) down to O(√n).",
    approach: [
      "Return false for anything less than 2 — this is a definition, not an edge case.",
      "Trial divide by every integer from 2 up to √n.",
      "Any exact division means composite.",
      "Surviving the loop means prime.",
      "Checking 2 and then only odd numbers halves the work again.",
    ],
    timeComplexity: "O(√n)",
    spaceComplexity: "O(1)",
    keyTakeaway:
      "The `n > 1` guard is part of the definition of prime, not defensive programming. Without it the loop simply never runs for 1, 0 and negatives, and they all come back as prime.",
    revisionSummary: [
      "Only test divisors up to √n — factors come in pairs.",
      "n < 2 is not prime, full stop.",
      "After 2, only odd divisors need testing.",
    ],
    criticalTrap:
      "The original has no lower-bound guard. For n = 1 the loop condition `i <= Math.sqrt(1)` is `2 <= 1`, so the body never executes and it returns **true**. Verified: 1, 0 and −7 are all wrongly reported prime.",
    leetcodeUrl: null,
    languages: ["JAVA", "PYTHON"],
    solutions: [
      { language: "JAVA", code: [
          "class Solution {",
          "    public boolean isPrime(int n) {",
          "        // Part of the definition: primes are greater than 1.",
          "        if (n < 2) {",
          "            return false;",
          "        }",
          "",
          "        if (n % 2 == 0) {",
          "            return n == 2;",
          "        }",
          "",
          "        // Only odd divisors remain, and only up to the square root.",
          "        for (int i = 3; (long) i * i <= n; i += 2) {",
          "            if (n % i == 0) {",
          "                return false;",
          "            }",
          "        }",
          "",
          "        return true;",
          "    }",
          "}",
        ].join("\n") },
      { language: "PYTHON", code: [
          "class Solution:",
          "    def is_prime(self, n: int) -> bool:",
          "        # Part of the definition: primes are greater than 1.",
          "        if n < 2:",
          "            return False",
          "        if n % 2 == 0:",
          "            return n == 2",
          "",
          "        i = 3",
          "        while i * i <= n:   # i*i avoids floating-point sqrt",
          "            if n % i == 0:",
          "                return False",
          "            i += 2",
          "",
          "        return True",
        ].join("\n") },
    ],
  },
  {
    slug: "math-sum-of-digits", number: null, title: "Sum the Digits of a Number", difficulty: "EASY",
    categorySlug: "math", patternSlugs: ["math"],
    synopsis: "Peel the last digit with % 10, drop it with / 10.",
    description: [
      "Return the sum of the decimal digits of a non-negative integer.",
    ],
    examples: [
      { label: "number", input: "n = 1234", output: "10", explanation: "1+2+3+4." },
      { label: "zero", input: "n = 0", output: "0", explanation: "A `while (n > 0)` loop never runs, which happens to give the right answer here." },
    ],
    constraints: [
      "0 ≤ n ≤ 2³¹ - 1",
    ],
    intuition:
      "The two-line digit loop that underpins the palindrome, reverse and base-conversion drills: `% 10` reads the last digit and `/ 10` removes it, so the number shrinks by one digit per iteration. Worth doing once on its own because the same loop reappears constantly, and because it makes the number of iterations obvious — one per digit, so O(log₁₀ n).",
    approach: [
      "While the number is greater than zero, take `% 10` and add it to a running total.",
      "Divide by 10 to discard that digit.",
      "Return the total.",
      "For negatives, take the absolute value first or the loop exits immediately.",
    ],
    timeComplexity: "O(log₁₀ n)",
    spaceComplexity: "O(1)",
    keyTakeaway:
      "One iteration per digit, not per unit. That distinction is why digit loops are logarithmic and why they are cheap even on large numbers.",
    revisionSummary: [
      "% 10 reads a digit, / 10 drops it.",
      "O(log₁₀ n) — one step per digit.",
      "Guard or absolute-value negatives, since `while (n > 0)` skips them.",
    ],
    criticalTrap:
      "`while (n > 0)` silently returns 0 for any negative input rather than reporting an error or summing the magnitude. Decide which you want and say so, because the loop hides the choice.",
    leetcodeUrl: null,
    languages: ["JAVA", "PYTHON"],
    solutions: [
      { language: "JAVA", code: [
          "class Solution {",
          "    public int sumOfDigits(int number) {",
          "        int sum = 0;",
          "        number = Math.abs(number);   // otherwise negatives return 0",
          "",
          "        while (number > 0) {",
          "            int lastDigit = number % 10;",
          "            sum = sum + lastDigit;",
          "            number = number / 10;    // drop the digit just read",
          "        }",
          "",
          "        return sum;",
          "    }",
          "}",
        ].join("\n") },
      { language: "PYTHON", code: [
          "class Solution:",
          "    def sum_of_digits(self, number: int) -> int:",
          "        total = 0",
          "        number = abs(number)   # otherwise negatives return 0",
          "",
          "        while number > 0:",
          "            total += number % 10",
          "            number //= 10          # drop the digit just read",
          "",
          "        return total",
        ].join("\n") },
    ],
  },
  {
    slug: "math-binary-to-decimal", number: null, title: "Convert Binary to Decimal", difficulty: "EASY",
    categorySlug: "math", patternSlugs: ["math"],
    synopsis: "Each digit weighted by its power of two.",
    description: [
      "Given a binary number, return its decimal value.",
    ],
    examples: [
      { label: "binary", input: "101", output: "5", explanation: "1×4 + 0×2 + 1×1." },
      { label: "eight bits", input: "11111111", output: "255", explanation: "Eight ones." },
    ],
    constraints: [
      "The input must contain only the digits 0 and 1",
    ],
    intuition:
      "Positional notation, read right to left: the digit at position *p* contributes `digit × 2^p`. So the same `% 10` / `/ 10` peeling loop works, with a running power that doubles each step. Using a shift or a running multiplier instead of `Math.pow` keeps it in integer arithmetic — `Math.pow` returns a `double`, and casting back invites rounding surprises for no benefit.",
    approach: [
      "Peel the last digit with `% 10`.",
      "Add `digit × 2^power` to the result.",
      "Increment the power and divide the input by 10.",
      "Prefer `1 << power`, or a multiplier that doubles, over `Math.pow`.",
      "Reject digits other than 0 and 1 if the input is untrusted.",
    ],
    timeComplexity: "O(number of bits)",
    spaceComplexity: "O(1)",
    keyTakeaway:
      "Keep it in integer arithmetic. `(int) Math.pow(2, p)` goes through floating point for a value that a single shift produces exactly.",
    revisionSummary: [
      "Digit at position p contributes digit × 2^p.",
      "Same %10 and /10 peeling, with a doubling weight.",
      "1 << p beats Math.pow — no floating point involved.",
    ],
    criticalTrap:
      "Holding the binary number in an `int`. Only ten binary digits fit before the decimal representation exceeds `Integer.MAX_VALUE`, so anything past 1111111111 is unrepresentable — a binary value really wants to arrive as a String.",
    leetcodeUrl: null,
    languages: ["JAVA", "PYTHON"],
    solutions: [
      { language: "JAVA", code: [
          "class Solution {",
          "    public int binToDec(int binNum) {",
          "        int pow = 0;",
          "        int decNum = 0;",
          "",
          "        while (binNum > 0) {",
          "            int lastDigit = binNum % 10;",
          "",
          "            // 1 << pow instead of (int) Math.pow(2, pow): exact, integer-only.",
          "            decNum = decNum + (lastDigit * (1 << pow));",
          "",
          "            pow++;",
          "            binNum = binNum / 10;",
          "        }",
          "",
          "        return decNum;",
          "    }",
          "",
          "    // What you actually want, since a binary value can be arbitrarily long.",
          "    public int binToDec(String bits) {",
          "        int decNum = 0;",
          "        for (int i = 0; i < bits.length(); i++) {",
          "            decNum = decNum * 2 + (bits.charAt(i) - \u00270\u0027);",
          "        }",
          "        return decNum;",
          "    }",
          "}",
        ].join("\n") },
      { language: "PYTHON", code: [
          "class Solution:",
          "    def bin_to_dec(self, bin_num: int) -> int:",
          "        pow_of_two = 1",
          "        dec_num = 0",
          "",
          "        while bin_num > 0:",
          "            last_digit = bin_num % 10",
          "            dec_num += last_digit * pow_of_two",
          "",
          "            pow_of_two *= 2      # doubling beats recomputing a power",
          "            bin_num //= 10",
          "",
          "        return dec_num",
          "",
          "    # Strings have no length limit, unlike an int.",
          "    def bin_str_to_dec(self, bits: str) -> int:",
          "        dec_num = 0",
          "        for ch in bits:",
          "            dec_num = dec_num * 2 + (ord(ch) - ord(\"0\"))",
          "        return dec_num",
        ].join("\n") },
    ],
  },
  {
    slug: "math-decimal-to-binary", number: null, title: "Convert Decimal to Binary", difficulty: "EASY",
    categorySlug: "math", patternSlugs: ["math"],
    synopsis: "Repeated division by two, read the remainders backwards.",
    description: [
      "Given a non-negative decimal integer, produce its binary representation.",
    ],
    examples: [
      { label: "small", input: "7", output: "111", explanation: "Remainders 1, 1, 1 read in reverse." },
      { label: "power of two", input: "1024", output: "10000000000", explanation: "Eleven characters — which is where packing the answer into an int breaks." },
    ],
    constraints: [
      "0 ≤ n ≤ 2³¹ - 1",
    ],
    intuition:
      "Dividing by two repeatedly and collecting the remainders produces the binary digits from least significant to most, so the answer is the remainders read backwards. The only real design decision is where to put them: building a *number* by adding `remainder × 10^power` reads naturally and breaks at 1024, whereas prepending to a string works for every input. Choosing the string is the point of the exercise.",
    approach: [
      "While the number is greater than zero, take `% 2` as the next bit.",
      "Divide by two and repeat.",
      "Prepend each bit to a string, or append and reverse at the end.",
      "Handle 0 explicitly — the loop produces nothing for it.",
    ],
    timeComplexity: "O(number of bits)",
    spaceComplexity: "O(number of bits) for the output string",
    keyTakeaway:
      "Return a String, not an int. A binary value has roughly 3.3× as many digits as its decimal form, so any input past 1023 overflows an int representation.",
    revisionSummary: [
      "Collect remainders of division by 2, then reverse.",
      "Output as a string — the digit count is the problem, not the value.",
      "Zero needs its own case.",
    ],
    criticalTrap:
      "Packing the binary digits into an `int` as `binNum + rem * 10^pow`, as the original does. It is exactly right up to 1023 and then breaks: verified, 1024 returns 2147483647 and 2047 returns −1036372538, both silently.",
    leetcodeUrl: null,
    languages: ["JAVA", "PYTHON"],
    solutions: [
      { language: "JAVA", code: [
          "class Solution {",
          "    // The original packs the answer into an int, which breaks past 1023.",
          "    public String decToBin(int decNum) {",
          "        if (decNum == 0) {",
          "            return \"0\";   // the loop emits nothing for zero",
          "        }",
          "",
          "        StringBuilder sb = new StringBuilder();",
          "",
          "        while (decNum > 0) {",
          "            int rem = decNum % 2;",
          "            sb.append(rem);        // append now, reverse once at the end",
          "            decNum = decNum / 2;",
          "        }",
          "",
          "        return sb.reverse().toString();",
          "    }",
          "}",
        ].join("\n") },
      { language: "PYTHON", code: [
          "class Solution:",
          "    def dec_to_bin(self, dec_num: int) -> str:",
          "        if dec_num == 0:",
          "            return \"0\"   # the loop emits nothing for zero",
          "",
          "        bits = []",
          "",
          "        while dec_num > 0:",
          "            bits.append(str(dec_num % 2))",
          "            dec_num //= 2",
          "",
          "        return \"\".join(reversed(bits))",
        ].join("\n") },
    ],
  },
  {
    slug: "math-binomial-coefficient", number: null, title: "Binomial Coefficient nCr", difficulty: "EASY",
    categorySlug: "math", patternSlugs: ["math"],
    synopsis: "Multiply and divide as you go — factorials overflow long before nCr does.",
    description: [
      "Compute the binomial coefficient C(n, r) = n! / (r! × (n − r)!), the number of ways to choose r items from n.",
    ],
    examples: [
      { label: "small", input: "n = 5, r = 2", output: "10", explanation: "5!/(2!3!) = 10." },
      { label: "larger", input: "n = 20, r = 10", output: "184756", explanation: "The answer fits easily in an int, but 20! does not." },
      { label: "symmetry", input: "n = 30, r = 15", output: "155117520", explanation: "Still an int-sized answer, from factorials far beyond long range." },
    ],
    constraints: [
      "0 ≤ r ≤ n",
    ],
    intuition:
      "The formula is a trap when read literally. C(30,15) is about 155 million — comfortably an int — but 30! is roughly 2.6 × 10³², which overflows even a `long` many times over. Computing the three factorials separately therefore destroys the answer before the division can recover it. Building the result incrementally as `res = res × (n − i) / (i + 1)` keeps every intermediate value near the size of the final answer, and each division is guaranteed to be exact at that point.",
    approach: [
      "Use the symmetry C(n, r) = C(n, n − r) to make r the smaller half.",
      "Start with `res = 1`.",
      "For each i from 0 to r − 1, multiply by `(n − i)` then divide by `(i + 1)`.",
      "The division is always exact in that order, so integer arithmetic is safe.",
      "Never compute the three factorials independently.",
    ],
    timeComplexity: "O(r)",
    spaceComplexity: "O(1)",
    keyTakeaway:
      "Interleave the multiply and divide. Intermediates then stay near the answer instead of ballooning to n!, which is the difference between working and overflowing at n = 13.",
    revisionSummary: [
      "C(n,r) = C(n, n-r) — always reduce r to the smaller half.",
      "res = res × (n-i) / (i+1), one step at a time.",
      "Separate factorials overflow int at 13! and long at 21!.",
    ],
    criticalTrap:
      "Computing `factorial(n) / (factorial(r) * factorial(n-r))` with `int` factorials, as the original does. 13! already exceeds int range, so the results go quietly wrong from n = 13: verified, C(13,6) returns 532 instead of 1716, C(20,10) returns 11 instead of 184756, and C(30,15) returns −4.",
    leetcodeUrl: null,
    languages: ["JAVA", "PYTHON"],
    solutions: [
      { language: "JAVA", code: [
          "class Solution {",
          "    // Incremental: intermediates stay near the answer, not near n!.",
          "    public long binCoeff(int n, int r) {",
          "        if (r < 0 || r > n) {",
          "            return 0;",
          "        }",
          "",
          "        r = Math.min(r, n - r);   // C(n, r) == C(n, n - r)",
          "        long res = 1;",
          "",
          "        for (int i = 0; i < r; i++) {",
          "            res = res * (n - i) / (i + 1);   // exact at every step",
          "        }",
          "",
          "        return res;",
          "    }",
          "}",
        ].join("\n") },
      { language: "PYTHON", code: [
          "class Solution:",
          "    def bin_coeff(self, n: int, r: int) -> int:",
          "        if r < 0 or r > n:",
          "            return 0",
          "",
          "        r = min(r, n - r)      # C(n, r) == C(n, n - r)",
          "        res = 1",
          "",
          "        for i in range(r):",
          "            res = res * (n - i) // (i + 1)   # exact at every step",
          "",
          "        return res",
        ].join("\n") },
    ],
  },
  {
    slug: "math-leap-year", number: null, title: "Check for a Leap Year", difficulty: "EASY",
    categorySlug: "math", patternSlugs: ["math"],
    synopsis: "Divisible by 4, except centuries, except every 400 years.",
    description: [
      "Return true if a given year is a leap year in the Gregorian calendar.",
    ],
    examples: [
      { label: "ordinary leap", input: "year = 2024", output: "true", explanation: "Divisible by 4 and not a century." },
      { label: "century, not leap", input: "year = 1900", output: "false", explanation: "Divisible by 100 but not by 400 — the case that catches a naive rule." },
      { label: "century, leap", input: "year = 2000", output: "true", explanation: "Divisible by 400, so the century exception is itself excepted." },
    ],
    constraints: [
      "1 ≤ year",
    ],
    intuition:
      "Three nested rules, and the whole question is whether you remember the third. Divisible by 4 is a leap year; unless it is also divisible by 100, which is not; unless it is further divisible by 400, which is. The reason is astronomical — a year is about 365.2422 days, so the quadrennial correction slightly overshoots and the century rule claws it back. Expressing it as `div4 && (not century || div400)` reads directly off that sentence.",
    approach: [
      "Compute `year % 4 == 0`.",
      "Compute `year % 100 != 0` — not a century year.",
      "Compute `year % 400 == 0` — the century exception.",
      "The answer is `div4 && (notCentury || div400)`.",
    ],
    timeComplexity: "O(1)",
    spaceComplexity: "O(1)",
    keyTakeaway:
      "Both exception layers are needed. Testing only divisibility by 4 makes 1900 a leap year; adding the century rule without the 400 rule makes 2000 an ordinary one.",
    revisionSummary: [
      "div4 && (not century || div400).",
      "1900 is not a leap year; 2000 is.",
      "Naive `% 4 == 0` is wrong three times per four centuries.",
    ],
    criticalTrap:
      "Stopping at `year % 4 == 0`. It is right for 96 years out of every 100 and wrong for 1700, 1800, 1900 and 2100 — the kind of rule that passes casual testing and fails in production a century later.",
    leetcodeUrl: null,
    languages: ["JAVA", "PYTHON"],
    solutions: [
      { language: "JAVA", code: [
          "class Solution {",
          "    public boolean isLeapYear(int year) {",
          "        boolean divisibleBy4 = (year % 4) == 0;",
          "        boolean notCentury = (year % 100) != 0;",
          "        boolean divisibleBy400 = (year % 400) == 0;",
          "",
          "        return divisibleBy4 && (notCentury || divisibleBy400);",
          "    }",
          "}",
        ].join("\n") },
      { language: "PYTHON", code: [
          "class Solution:",
          "    def is_leap_year(self, year: int) -> bool:",
          "        divisible_by_4 = year % 4 == 0",
          "        not_century = year % 100 != 0",
          "        divisible_by_400 = year % 400 == 0",
          "",
          "        return divisible_by_4 and (not_century or divisible_by_400)",
        ].join("\n") },
    ],
  },
];
